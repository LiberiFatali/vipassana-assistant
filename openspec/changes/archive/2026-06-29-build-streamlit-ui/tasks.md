## 1. Setup and Dependencies

- [x] 1.1 Add `streamlit` dependency to `pyproject.toml` and run lock/sync.

## 2. Streamlit Web UI Implementation

- [x] 2.1 Create `chatbot_agent/streamlit_app.py` with basic page configuration.
- [x] 2.2 Implement session state to cache the agent instance and chat history.
- [x] 2.3 Build the UI components including the chat window, sidebar instructions, and clear-chat button.
- [x] 2.4 Add safe URL verification/filtering and dynamic warnings for fallback course schedules in the UI.

## 3. Deployment and Verification

- [x] 3.1 Create a Dockerfile optimized for running the Streamlit application.
- [x] 3.2 Verify the Streamlit app runs locally and integrates with the underlying agent.
