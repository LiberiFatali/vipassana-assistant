{
  "name": "vipassana-ucenlist-agent",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Bilingual Vipassana UCENLIST chatbot agent deployed on Vercel with OpenCode Zen LLM access.",
  "scripts": {
    "dev": "node --env-file=.env server.js",
    "start": "node --env-file=.env server.js",
    "test": "node --test tests/*"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "wink-bm25-text-search": "^3.1.2",
    "zod": "^4.4.3"
  }
}
